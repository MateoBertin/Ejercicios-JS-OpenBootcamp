let alt_cm = 183;
let alt_m = 1.83;
let peso = 50.5
let alt_arriba = alt_m.toFixed()
console.log(alt_arriba);
let peso_abajo = Math.floor(peso)
console.log(peso_abajo);
let max_val = Number.MAX_VALUE + 1 == Number.MIN_VALUE
console.log(max_val);